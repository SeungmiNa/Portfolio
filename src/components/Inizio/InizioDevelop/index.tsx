import React from "react";

interface injectedProps {}

const InizioDevelop: React.FC<injectedProps> = ({}) => {
  return (
    <div>
      <p className="text-white">
        Produced and assisted in building user interfaces' components and pages
        with Typescript and TailwindCss
      </p>
    </div>
  );
};

export default InizioDevelop;
